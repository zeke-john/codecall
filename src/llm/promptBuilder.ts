/**
 * promptBuilder.ts
 * 
 * Constructs system prompts and user messages for the model.
 * Ensures prompts are well-structured and provide necessary context.
 * 
 * Responsibilities:
 * - Build system prompt with instructions
 * - Format user requests with context
 * - Inject SDK file tree and contents
 * - Format execution traces for error recovery
 * - Keep token count reasonable
 */

// TODO: Implement prompt builder
